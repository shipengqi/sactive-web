const DOWNLOAD_ACCEPT = 'application/x-download';

const ERROR_CODE = {
  UNIFIED: 500,
  NOT_FOUNF: 404,
  VALIFATE_FAIL: 400
};

const SUCCESS_CODE = {
  UNIFIED: 200
};

module.exports = {
  DOWNLOAD_ACCEPT,
  ERROR_CODE,
  SUCCESS_CODE
};